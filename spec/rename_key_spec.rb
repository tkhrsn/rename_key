require 'spec_helper'

describe RenameKey do
  it 'has a version number' do
    expect(RenameKey::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
